<%- include('partials/header.ejs'); -%>

<h1>Home</h1>
<p><%= pageContent %></p>

<% if ( posts.length > 0) { %>
  
  <% for (var i = 0; i < posts.length; i++) { %>
  <div>
    <h2><%= _.capitalize(posts[i].title) %></h2>
    <p><%= _.truncate(posts[i].content,{ 'length': 100, 'omission': '…' }) %> <a href="/posts/<%= posts[i].title %>">Read More</a></p>
  </div>
  <% } %>
<% } %>
  

<%- include('partials/footer.ejs'); -%>
